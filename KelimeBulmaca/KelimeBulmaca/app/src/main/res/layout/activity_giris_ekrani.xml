<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/splashscreendesign"
    tools:context=".giris_ekrani">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/logo"
        android:layout_marginTop="40dp"
        android:padding="50dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">
    </ImageView>

    <ProgressBar
        android:id="@+id/yükleniyor"
        style="@style/MaterialAlertDialog.MaterialComponents.Title.Panel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="80dp"
        android:min="0"
        android:max="100"
        android:foregroundGravity="center"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">
    </ProgressBar>

    <TextView
        android:id="@+id/txt_durum"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:textColor="@color/durum"
        android:layout_marginTop="5dp"
        app:layout_constraintLeft_toLeftOf="@+id/yükleniyor"
        app:layout_constraintRight_toRightOf="@+id/yükleniyor"
        app:layout_constraintTop_toBottomOf="@+id/yükleniyor">
    </TextView>

</androidx.constraintlayout.widget.ConstraintLayout>